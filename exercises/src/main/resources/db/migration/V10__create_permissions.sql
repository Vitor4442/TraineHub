CREATE TABLE IF NOT EXISTS permissions (
                                           id              BIGSERIAL PRIMARY KEY,
                                           description     VARCHAR(255) DEFAULT NULL
    );
